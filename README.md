# Sylvester · Channel Drops

Public asset library for the channel. Assets are stored in Cloudflare R2; this
static site (GitHub Pages) lists them from `drops.json`. The Telegram bot
(`/drop`) uploads to R2 and updates `drops.json` here via the GitHub API.

- Live site: https://fmssylvester.github.io/channel-drops/
